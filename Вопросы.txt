1.	Function Declaration, Function Expression и Named Function Expression.
2.	Просто вызвать функцию через ее название, например «SAYHI()» и функция может вызвать саму себя, если вписать ее название в внутрь в само тело функции.
3.	Тестирование-это проверка всего функционала, который в итоге получается, после написания кода(например, тыкаем по всем кнопочкам, вводим во все «инпуты» какие-нибудь слова), а отладка-это проверка самого кода на правильное написание и его работоспособность. Логирование-это какая-то запись, которая позволяет понять, что происходит при коде и при каких обстоятельствах, позволяет понять, что происходит в программе
4.	Function Declaration-интерпретатор самостоятельно присваивает переменной функцию, которую мы пишем в коде, а с Function Expression мы сами объявляем переменную с именем функции и присваиваем ей описание функции.
5.	Выводит результат в консоль (вызов в браузере f12)
6.	Когда движок JavaScript готовится выполнять скрипт или блок кода, прежде всего он ищет в нём Function Declaration и создаёт все такие функции. И только после того, как все объявления Function Declaration будут обработаны, продолжится выполнение.
7.	Область видимости вне любых функций называется глобальной. Переменные, объявленные в ней, видны везде:
8.	28
9.	5
10.	Результат будет ошибкой, так как мы пытаемся сначала вывести в консоль значение переменной или функции, которое еще не задано